pip install graphify  
pip install hound-mcp==12.4.1  
npm install -g bladebro  
chromium 144+ and chrome://inspect/#remote-debugging enabled

"Please setup scheduled job for the scripts/server-launch.ps1 in my opencode config folder for startup"